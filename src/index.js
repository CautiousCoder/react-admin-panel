/*
 * Title: Admin Panel Project
 *Description: Here, we create an admin panel where a user is act as Create, Edit, Update and Delete a Product. Moreover, a user can see differt type of graph or chat in which provide some vulnerable information to user.
 *Author : MD. ZAHIDUL ISLAM
 *Description:  17/12/2023
 */

import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";
// import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
// reportWebVitals();
